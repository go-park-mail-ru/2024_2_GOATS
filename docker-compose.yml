version: '3'

services:
  postgres:
    image: postgres:13.2
    environment:
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '5432:5432'

  redis:
    image: 'redis'
    ports:
      - '6379:6379'
